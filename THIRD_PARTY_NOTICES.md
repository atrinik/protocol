# Third-party notices

The checked-in Go bindings use the Go Protobuf runtime (BSD-3-Clause). The Rust
crate uses Prost (Apache-2.0) and Bytes (MIT). Exact locked transitive versions
and their SPDX expressions are emitted in release SBOMs. Buf, protoc,
protoc-gen-go, and protoc-gen-prost are build tools and retain their upstream
licenses; generated code is derived from this repository's MIT schemas.

No GPL/AGPL dependency, classic Atrinik binding, authored content, visual
asset, audio, or runtime data is bundled.
